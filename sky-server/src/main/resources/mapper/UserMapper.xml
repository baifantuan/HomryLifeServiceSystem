<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.UserMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into user (openid, name, phone, sex, id_number, avatar, create_time)
        values (#{openid}, #{name}, #{phone}, #{sex}, #{idNumber}, #{avatar}, #{createTime})
    </insert>

    <select id="getByOpenId" resultType="com.sky.entity.User">
        select * from user where openid = #{openid}
    </select>

    <select id="getById" resultType="com.sky.entity.User">
        select * from user where id = #{userId}
    </select>

    <select id="sumOfNewUser" resultType="java.lang.Integer">
        select sum(id) from user where create_time > #{begin} and #{end} > create_time
    </select>

    <select id="sumOfAllUser" resultType="java.lang.Integer">
        select sum(id) from user where #{begin} > create_time
    </select>


</mapper>